{% set name = "LbAPCommon" %}
{% set version = "0.6.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/LbAPCommon-{{ version }}.tar.gz
  sha256: 2632e7f4c6187ebfb23510bd9453cd4158504f9b2c37c7655cc1e46eea221f42

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.9
    - setuptools-scm
  run:
    - awkward >=1.0.0
    - beautifulsoup4
    - boost-histogram
    - hist
    - jinja2
    - lxml
    - matplotlib-base
    - python >=3.9
    - pyyaml
    - requests
    - requests-gssapi
    - requests-kerberos
    - setuptools
    - strictyaml
    - uproot >=4.0.0
    - xrootd

test:
  imports:
    - LbAPCommon
    - LbAPCommon.cern_sso
    - LbAPCommon.linting
    - LbAPCommon.validators
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://gitlab.cern.ch/lhcb-dpa/analysis-productions/LbAPCommon
  summary: Common utilities used by LHCb DPA WP2 related software
  dev_url: https://gitlab.cern.ch/lhcb-dpa/analysis-productions/LbAPCommon
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - AidanWiederhold
    - chrisburr
